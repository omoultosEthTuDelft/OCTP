### Install postprocessing python package
The project is published on pypi: https://pypi.org/project/octp-pp/
Therefore, the easy install is just:

```bash
pip install octp-pp
```

See [example_use.ipynb](example_use.ipynb) for examples